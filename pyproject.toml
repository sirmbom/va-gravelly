[project]
name = "gravelly-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "vision-agents[gemini,getstream]>=0.6.1",
    "python-dotenv>=1.0.1",
]
